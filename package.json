{
  "name": "react-autosuggest-sample",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@material-ui/core": "^4.2.0",
    "autosuggest-highlight": "^3.1.1",
    "material-ui-chip-input": "^1.0.0-beta.17",
    "react": "^16.8.6",
    "react-autosuggest": "^9.4.3",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "PUBLIC_URL=/react-autosuggest-sample/ yarn build && cd build && git init && git add -A && git commit -m 'deploy app' && git push -f git@github.com:ozaki25/react-autosuggest-sample.git master:gh-pages"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
